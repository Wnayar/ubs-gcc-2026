# Shared secret guarding /debug/requests on the deployed instance.
# Render generates its own value (see render.yaml). Leave unset locally
# and the endpoint is open for dev convenience.
DEBUG_TOKEN=
